apiVersion: apps/v1
kind: Deployment

metadata:
  name: frontend
  namespace: online-book-store

spec:
  replicas: 2

  selector:
    matchLabels:
      app: frontend

  template:
    metadata:
      labels:
        app: frontend

    spec:
      containers:
      - name: frontend

        image: YOUR_DOCKERHUB_USERNAME/bookstore-frontend:latest

        imagePullPolicy: Always

        ports:
        - containerPort: 80